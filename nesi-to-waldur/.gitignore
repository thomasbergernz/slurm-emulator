.venv/
__pycache__/
*.egg-info/

# Contain real NeSI PII (emails, names) and Waldur action logs once populated
# against a real run — keep the directories, not their generated contents.
manifests/*.json
reports/*.json
